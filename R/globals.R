# run checkhelper::print_globals() to get the globalVariables

globalVariables(unique(c(
  # name_be_extract:
  "ID_detect_unique", "n_tot", "name_detect_noms", "name_detected", "name_extracted", "noms_65",
  # name_be_genderize:
  "dico", "dist_fuzzy", "distance_jw", "ID_name_unique", "min_jw", "name_detected", "name_join_data", "name_join_noms", "noms_65"
)))
